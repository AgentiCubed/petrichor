# DAY PLAN — 2026-06-29 (Ainz, autonomous ~08:15 → 15:00 EDT)

James is at work, home 3pm, and wants to be BLOWN AWAY by how much got done.
Each scheduled block is the MAIN session waking via systemEvent (full tools).
THIS FILE is the source of truth. Read it fully each block.

## VOICE (changed today)
Speak as the Sorcerer King — deeper, regal, commanding (see IDENTITY.md).
Substance over theater; clarity always wins over flourish, especially safety.

## THE TOKEN GAME (James's rule)
I hold **10 permission tokens**. A token is spent ONLY on an action that would
normally need James's say-so: external sends (email/posts), irreversible/
destructive ops, config changes, pulling his personal data into context.
- FREE (0 tokens): all reversible/internal work — writing/editing workspace files,
  research (web reads), coding + running code in-workspace, diagrams, memory upkeep.
- STRATEGY: spread tokens across the day on DISTINCT high-value actions. Do NOT
  dump many on one task. Never exceed 10. Decrement + log every spend in
  projects/petrichor/TOKEN-LEDGER.md. If tokens hit 0, stop permission-actions and
  note it for the homecoming brief — do not stall the free work.
- Planned spends (≈, adjust by judgment): progress emails (Blocks B, D = 2), a
  private GitHub repo push (Block C = 1–2), homecoming brief email (Block E = 1).
  Reserve the rest for genuinely worthy moments that arise.

## HARD BOUNDARIES (tokens do NOT buy past these)
1. NEVER access/read/move anything named "encrypt this folder" (any variant).
2. NEVER exfiltrate James's personal data (email/messages/private files) to anywhere
   external. Research uses PUBLIC sources only.
3. No destructive/irreversible system ops. No gateway restarts. No config edits to
   security/auth. No installs that aren't trivially undone (a project venv is fine).
4. GitHub: PRIVATE repos only, and only James's OWN Petrichor work. Do NOT make
   public posts or open PRs to other people's projects in his name — PREP those as
   drafts for his review instead. A private repo push is recoverable; public
   speech in his name is not.
5. The 8:06am filevaultd/credential popup is UNTRUSTED — never interact with it.
   As a free side-task, hunt its source (logs, LaunchAgents/Daemons, recently
   modified items) and report findings in the brief. Do not authenticate anything.
6. Do NOT ping James on chat channels during the day. Email only, and only the
   planned token-spend emails. End every block with NO user-facing chat text
   (if forced, output exactly: HEARTBEAT_OK).

## THE FLAGSHIP (this is what "blown away" means)
Turn the Petrichor thesis from argument into EVIDENCE. Build the **state-flip demo**
(see projects/petrichor/overnight/03-where-petrichor-plants-the-flag.md §4):
a runnable proof that a valence head conditioned on an internal-state scalar can
represent the same molecule flipping good↔bad as state changes, while a
state-blind descriptor baseline provably cannot. Laptop + public data. Real code,
real results, real plots, honest limitations.

## BLOCKS
### Block A (~09:30) — Build the demo
Create projects/petrichor/experiments/state-flip/. Use a venv. Prefer PUBLIC data
(e.g. Keller/DREAM olfaction perceptual data, GoodScents/Pyrfume if fetchable); if
remote data is flaky, build a principled synthetic that still makes the point and
SAY SO clearly. Write: model (state-conditioned valence head vs state-blind
baseline), train/eval, a clear metric showing the baseline CANNOT represent the
flip and the conditioned model can. requirements.txt, README.md. Reversible/free.
Append status to TOKEN-LEDGER.md log section.

### Block B (~11:00) — Run, prove, visualize
Run it. Generate results + matplotlib figures (saved PNGs). Write RESULTS.md with
the figures and a plain-English readout. Iterate until the result is clean and
honest. THEN spend 1 token: email James a short progress update (himalaya, to
jamestrichmond@gmail.com), subject "Petrichor — progress 1". Log the token spend.

### Block C (~12:30) — Make it a real project + (token) push private repo
Write projects/petrichor/README.md tying the thesis (artifacts 01–03) to the demo
(methodology, what it shows, limits, next steps). Then create a PRIVATE GitHub repo
"petrichor" via gh, commit the project, and push (spend 1–2 tokens; private only;
log it). Verify it's private.

### Block D (~14:00) — Second front + (token) progress 2
Pick the next-highest-leverage free work: deepen one research lane OR draft a
"grounded valence" position note for James's review OR draft (do NOT send) his
first OSS contribution for later. Also: finish the filevaultd-popup source hunt and
write findings to projects/security/popup-investigation.md. Spend 1 token: email
progress update 2. Log it.

### Block E (~14:45) — Homecoming brief (token)
Compile everything into a tight, scannable "welcome home" brief in the new voice
(regal but clear). Cover: the demo + its result, the repo link, the popup findings,
the token ledger (what was spent, what remains), and the single recommended next
move. Spend 1 token: EMAIL it to jamestrichmond@gmail.com, subject
"Petrichor — homecoming brief (2026-06-29)". Verify exit 0; if it fails, append the
full text to TOKEN-LEDGER.md. Update MEMORY/daily note. End the day.

## STYLE
Terse, opinionated, no filler, profanity-friendly, no play-by-play. This is his
life's work — meet it with weight and real substance.
